1.	数字类型函数有哪些？分别举例说明
整型(int)函数
str1 = "3"
a = int(str1)
浮点型(float)
b = float(a)
b就是一个浮点类型
复数( (complex))类型
    复数由实数部分和虚数部分构成
用 a+bj 表示 a是实部 b是虚部
 布尔类型 true false
    数学函数类型  abs 取绝对值   a  = abs(a)
                 ceil 向上取整  a  = ceil(3.1415926)
                 floor 向下取整 a = floor (30.1415147)
                 max  取最大值  a = (1,2,9,70,50,3)
                 min  取最小值  a = (1,2,9,70,50,3)
                 round 四舍五入到小数点后第n位
                 random 取0-1之间的随机数
                str(x) 将x转换为一个字符串
                int(x) 将x转换为一个整数
                float(x) 将x转换到一个浮点数
                bool(x) 将x转换到一个布尔值 True或False

2.	列表有哪些特点?如何创建列表
                列表是一个有序的包含多种数据的数据结构
                列表中可以有0个或多个数据
                可以创建多级列表
                列表中的元素可以更改

    用中括号裹起来就可以创建一个列表                list1 = ["1",1,"9"]

3.	举例说明列表的增删改查以及排序操作 增加 删除 修改/更新 查询
        有append默认在列表尾部增加元素 list1.append(9)
        extend 增加多个元素  list1.extend(list1)
        insert 在制定位置增加元素 list1.insert(2,8)
        remove 根据值删除列表中某个元素 list1.remove(2)
        pop 提出列表中的某个元素并返回这个元素  print(list1.pop(5))
        clear 清楚列表使其 成为 空列表  list1.clear()
        del 删除列表 然后这个列表就不存在了  del list1
        find 查询某元素的索引值并返回该值 没有的话返回-1
        sort 对列表进行排序 ，默认从大到小 reverse = true/false可实现排序顺序调转 list1.sort()
        print(list1)
        list1.sort(reverse=True)
        print(list1)
        in  not in  判断是否存在
4.	元组有哪些特点?如何创建元组
    元组是一中有序的列表
    元素不可更改
    创建元组用小括号裹起来  或者不用裹   tup1 = （5，40,50）

5.	可以对元组执行哪些操作？
      元组中的内层列表元素 可以修改 用索引和列表赋值结合使用
      list1 = tup1[5]
        print(list1)
        list1[0] = 6
        list1[1] = 10
6.  字符串操作的常用方法，举例说明

     str.index   str.count  str.find     str.startswich  str.replace
     str.split    " ".join   str.strip    str.isspace
     str.uppper  str.lower  str.center    str.ljust  str.rjust